'use client';
import Image from 'next/image';import Link from 'next/link';import {Trash2} from 'lucide-react';import {useCart} from '@/components/providers/CartProvider';import {euro} from '@/lib/format';
export default function Cart(){const{items,subtotal,remove,setQty}=useCart();const shipping=subtotal>=60||subtotal===0?0:5.90;return <section className="mx-auto max-w-6xl px-4 py-12 sm:px-6"><h1 className="text-4xl font-bold">Tu carrito</h1>{!items.length?<div className="mt-10 rounded-2xl border border-dashed border-white/15 p-12 text-center"><p className="text-zinc-400">Aún no has añadido ninguna pieza.</p><Link href="/catalogo" className="mt-5 inline-flex rounded-xl bg-white px-5 py-3 font-semibold text-zinc-950">Explorar catálogo</Link></div>:<div className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><div className="space-y-3">{items.map(x=><div key={`${x.product.id}-${x.color}-${x.material}`} className="flex gap-4 rounded-2xl border border-white/10 p-4"><Image src={x.product.image} alt={x.product.name} width={130} height={100} className="h-24 w-28 rounded-xl object-cover"/><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h2 className="font-semibold">{x.product.name}</h2><p className="mt-1 text-xs text-zinc-500">{x.material} · {x.color}</p></div><button onClick={()=>remove(x.product.id)} aria-label="Eliminar" className="self-start rounded-lg p-2 text-zinc-500 hover:bg-white/10 hover:text-white"><Trash2 size={17}/></button></div><div className="mt-4 flex items-center justify-between"><input aria-label="Cantidad" type="number" min={1} value={x.quantity} onChange={e=>setQty(x.product.id,Number(e.target.value))} className="w-16 rounded-lg border border-white/10 bg-zinc-900 p-2"/><strong>{euro.format(x.product.price*x.quantity)}</strong></div></div></div>)}</div><aside className="h-fit rounded-2xl border border-white/10 bg-zinc-900/60 p-6"><h2 className="text-xl font-semibold">Resumen</h2><div className="mt-5 space-y-3 text-sm"><div className="flex justify-between text-zinc-400"><span>Subtotal</span><span>{euro.format(subtotal)}</span></div><div className="flex justify-between text-zinc-400"><span>Envío</span><span>{shipping?euro.format(shipping):'Gratis'}</span></div><div className="flex justify-between border-t border-white/10 pt-4 text-lg font-bold"><span>Total</span><span>{euro.format(subtotal+shipping)}</span></div><p className="text-xs text-zinc-500">IVA incluido. El envío se calcula antes del pago.</p></div><Link href="/checkout" className="mt-6 flex w-full justify-center rounded-xl bg-white p-3 font-semibold text-zinc-950 hover:bg-cyan-300">Continuar al checkout</Link></aside></div>}</section>}
